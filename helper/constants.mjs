// Mirrors MAX_SEATS and MAX_HELPER_PEERS in lib/sync.ts; a .mjs helper cannot import the .ts module. Keep them equal.
export const MAX_SEATS = 12;
export const MAX_HELPER_PEERS = 12;
export const MAX_ROOM_TORRENTS = 2;
